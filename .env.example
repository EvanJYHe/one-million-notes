# Optional canonical URL used by metadata generation.
NEXT_PUBLIC_SITE_URL=https://one-million-notes.evan-he24.workers.dev

# Optional server-side fallback for hosts without a native D1 binding.
NOTES_BACKEND_ORIGIN=

# Optional server-side moderation. Leave unset to use local validation only.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
